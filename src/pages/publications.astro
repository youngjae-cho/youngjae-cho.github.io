---
import Base from '../layouts/Base.astro';
import PubCard from '../components/PubCard.astro';
import { publications } from '../data/publications';

const pubs = [...publications].sort((a, b) => b.date.localeCompare(a.date));
---
<Base title="Publications — Youngjae Cho" active="publications">
  <main class="wrap">
    <header class="page-head">
      <div class="eyebrow">Publications</div>
      <h1>Publications</h1>
      <p class="intro">Six papers on robust learning under imperfect supervision — four first-author (incl. one co-first), spanning ICML, AAAI, IEEE SMC, and arXiv. Each one is tagged with the supervision failure it attacks: noisy labels, scarce labels, distribution shift, spurious correlation.</p>
    </header>
    <section class="block" style="border-bottom:0">
      <div class="grid">
        {pubs.map((p) => <PubCard pub={p} />)}
      </div>
    </section>
  </main>
</Base>
